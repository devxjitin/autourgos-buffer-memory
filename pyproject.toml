[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "autourgos-buffer-memory"
version = "1.0.0"
description = "In-memory short-term buffer for Autourgos agents — RuntimeShortTermMemory, ConversationBufferMemory."
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
authors = [
    {name = "Jitin Kumar Sengar", email = "autourgoskit@gmail.com"}
]
keywords = ["autourgos", "agent", "memory", "llm"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]
dependencies = []

[project.urls]
Homepage = "https://pypi.org/project/autourgos-buffer-memory/"
Repository = "https://github.com/jitin-kumar-sengar/autourgos"
"Bug Tracker" = "https://github.com/jitin-kumar-sengar/autourgos/issues"

[tool.setuptools.packages.find]
where = ["."]
include = ["autourgos_buffer_memory*"]

[tool.setuptools.package-data]
autourgos_buffer_memory = ["py.typed"]
